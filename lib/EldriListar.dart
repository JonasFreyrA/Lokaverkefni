import 'package:flutter/material.dart';

class EldriListar extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Hvað var keypt síðast')),
      body: Center(
        child: Text('þetta var keypt síðast'),
      ),
    );
  }
}