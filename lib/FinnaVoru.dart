import 'package:flutter/material.dart';

class InnkaupaListi extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Finna vöru')),
      body: Center(
        child: Text('Velja'),
      ),
    );
  }
}